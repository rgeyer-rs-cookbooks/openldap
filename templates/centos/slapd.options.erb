SLAPD_LDAPI=yes

SLAPD_LDAP=yes

#SLAPD_URLS="ldap://<% if !@listen_host.empty? && !@listen_port.empty? -%><%= @listen_host %>:<%= @listen_port %><% end %>/ ldapi:///"

#SLAPD_OPTIONS=
#SLAPD_SHUTDOWN_TIMEOUT=3
#SLAPD_ULIMIT_SETTINGS=""